ALTER TABLE ACTIVITY
    ADD STATUS CHAR(1) NOT NULL DEFAULT ('S');
COMMENT ON COLUMN ACTIVITY.STATUS IS 'P - PUBLICATED, D - DELETED, S - Sketch';

ALTER TABLE PERSON
    ADD IMAGE_ID INT REFERENCES IMAGE;